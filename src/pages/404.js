import React from "react"
import Layout from "../components/Layout"
const error = () => {
  return (
    <Layout>Oh oh, the content you are looking for does not exist yet.</Layout>
  )
}

export default error
